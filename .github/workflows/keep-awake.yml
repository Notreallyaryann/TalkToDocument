name: Keep Render App Awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  keep-awake:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render App
        run: |
          curl --silent --show-error --fail ${{ secrets.RENDER_APP_URL }} || echo "Ping failed"
